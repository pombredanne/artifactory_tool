.. highlight:: shell

============
Installation
============

At the command line::

    $ easy_install artifactory_tool

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv artifactory_tool
    $ pip install artifactory_tool
